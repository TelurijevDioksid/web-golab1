<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>goQR</title>
</head>
<style>
    body {
        margin: 0;
        padding: 0;
        height: 100vh;
        width: 100vw;
        background-color: #f0f0f0;
        color: #000000;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    a {
        text-decoration: none;
        border: none;
        border-radius: 0.5rem;
        padding: 0.5rem 1rem;
        background-color: #000000;
        font-size: 1.5rem;
        color: #ffffff;
    }
</style>
<body>
    <h3>Broj generiranih ulaznica</h3>
    <h1>{{.Total}}</h1>
    <a href="{{.BtnUrl}}">{{.BtnMsg}}</a>
</body>
</html>
